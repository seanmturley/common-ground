import JoinQueue from "@components/join-queue";
import type { Meta, StoryObj } from "@storybook/react";

export default {
  title: "JoinQueue",
  component: JoinQueue
} satisfies Meta<typeof JoinQueue>;

type Story = StoryObj<typeof JoinQueue>;

export const Default: Story = {};
