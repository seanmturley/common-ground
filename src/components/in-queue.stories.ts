import InQueue from "@components/in-queue";
import type { Meta, StoryObj } from "@storybook/react";

export default {
  title: "InQueue",
  component: InQueue
} satisfies Meta<typeof InQueue>;

type Story = StoryObj<typeof InQueue>;

export const Default: Story = {};
